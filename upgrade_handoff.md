# 资金账本交接 · 1.0.0

## 项目边界

`my-assets` 是独立项目、独立 Git 仓库。原 `my-ledger` 保留收支记账，本项目仅管理资产余额；不迁移或改写原账本数据。

## 已实现的需求

- 自填资产来源，日元 / 人民币两种币种，余额可为负。
- 大币种切换按钮同时控制新建资产默认币种和折算展示币种。
- 余额替换而非累计流水；编辑保存自动改为当天，旧日快照保留。
- 每日总资产日历与折线图；未更新日期延续上次快照，不给首次登记前日期补零。
- Frankfurter 自动获取最新参考价，离线缓存、手动备用；明确展示报价日期，不宣称秒级行情。
- 保存资产时保存当时汇率；历史不受之后汇率波动影响。支持单独保存今日快照。
- 本地 IndexedDB、跨窗口变更提醒与旧版本写入保护、JSON 备份导入导出、双确认清空、深色模式、独立 PWA。

## 关键文件

- `src/domain/ledger.ts`：最小货币单位整数存储、双币转换、快照、日期延续。
- `src/services/database.ts`：单事务保存完整状态与 revision 冲突保护。
- `src/services/rates.ts`：汇率请求和缓存。
- `src/services/backup.ts`：备份格式、导入校验。
- `src/App.tsx` / `src/App.css`：四个页面与界面。
- `src/components/AssetEditor.tsx` / `History.tsx`：余额编辑器和资产日历。
- `tests/ledger.test.ts`：六项关键数据逻辑测试，不含浏览器巡检。
- `.github/workflows/deploy.yml`：独立 Pages 部署。

## 数据约定

JPY 整数，CNY 以分存储。汇率按 1 CNY 对应 JPY 表示。先汇总原币，再折算并对称四舍五入，避免逐条转换累积误差。同币种来源规范化后唯一。日期使用设备本地日期；设备日期早于现有快照时拒绝保存，避免损坏历史顺序。

缺少汇率时保留原币金额，跨币种合计显示待汇率；历史不自动补汇率。删除只改变当天和之后的持仓。JSON 恢复要求来自本应用且快照总额与明细一致。

新数据库：`my-assets-db`，新存储键前缀：`my-assets-`，PWA 路径：`/my-assets/`。同域浏览器清理仍可能同时影响两个应用，请分别备份。

## 验收范围

按用户要求仅保留关键数据测试和构建；外观、手机安装、日常交互由用户亲自验收。不执行全量回归或自动浏览器巡检。
